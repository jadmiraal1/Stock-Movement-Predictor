name: stock-predictor-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pandas
  - numpy
  - scikit-learn
  - matplotlib
  - seaborn
  - yfinance
  - pip
  - pip:
      - vaderSentiment
      - torch
      - transformers
      - alpaca-trade-api
      - tqdm
      - python-dotenv
